{
  "name": "kliktahu-skema",
  "version": "2.0.0",
  "private": true,
  "description": "Tipe data TypeScript (dihasilkan dari kliktahu/skema.py) + rumus skor KlikTahu (port identik kliktahu/skor.py, diuji paritas). BUKAN aplikasi.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/skor.test.ts",
    "cek": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "typescript": "7.0.2"
  }
}
